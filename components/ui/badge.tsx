const Badge=({children}:any)=>{
    return(
        <span className="inline-flex items-center rounded-md bg-pink-50 bg-gradient-to-r from-indigo-500 from-10% via-sky-500 via-30% to-emerald-500 to-90% px-2 py-1 text-xs font-medium">{children}</span>

    )
}
export default Badge